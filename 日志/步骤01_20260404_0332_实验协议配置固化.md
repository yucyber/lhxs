# 步骤01：实验协议配置固化

- 时间：2026-04-04 03:32
- 本步骤目标：把原论文实验协议和当前默认可运行参数固化到 `configs/*.yaml`，为后续算法实现和实验调度提供统一配置入口。
- 本步骤新增/修改文件：
  - 新增 `configs/algorithms.yaml`
  - 新增 `configs/strong_phase.yaml`
  - 新增 `configs/weak_phase.yaml`
  - 新增 `configs/step_size_compare.yaml`
  - 新增 `configs/rank_aware_compare.yaml`
  - 新增 `日志/步骤01_20260404_0332_实验协议配置固化.md`
- 本步骤执行的命令：
  - 读取 `docs/reference_paper/Greedy_Algorithms_for_Joint_Sparse_Recovery_clean.md` 中 Algorithm 1-3、strong phase transitions、weak phase transitions、stopping criteria 相关段落
  - 检查 `.venv\Scripts\python.exe -m pip --version`
- 自检结果：5 份配置文件已生成；每份都保留 `paper_reference` 与 `run` 两层，避免把“原论文协议”和“当前快速默认运行参数”混在一起。
- 遇到的问题与判断：原论文 clean.md 未直接给出 [21] 中 `L(delta,rho), U(delta,rho)` 的闭式公式，因此 `configs/strong_phase.yaml` 中已明确标记强相变理论曲线当前默认使用 `surrogate_arip_v1` 代理边界模型，后续若补齐 [21] 公式需在 STEP 5 替换实现并记录日志。
- 下一步建议：进入 STEP 2，实现 `core/` 公共模块、停止准则、成功判据和基础测试。
