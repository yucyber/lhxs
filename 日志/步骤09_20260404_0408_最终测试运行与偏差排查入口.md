# 步骤09：最终测试运行与偏差排查入口

- 时间：2026-04-04 04:08
- 本步骤目标：安装实验依赖，运行全部单元测试和总控实验脚本，生成最终图表/表格/报告，并补充复现偏差排查入口。
- 本步骤新增/修改文件：
  - 修改 `src/joint_sparse_recovery/core/metrics.py`
  - 新增 `outputs/reports/复现偏差排查入口.md`
  - 新增 `日志/步骤09_20260404_0408_最终测试运行与偏差排查入口.md`
  - 修改 `README.md`
- 本步骤执行的命令：
  - `.\\.venv\\Scripts\\python.exe -m pip install numpy scipy matplotlib`
  - `.\\.venv\\Scripts\\python.exe -m unittest discover -s tests`
  - `.\\.venv\\Scripts\\python.exe scripts\\run_all_experiments.py`
- 自检结果：
  - 依赖安装成功：numpy/scipy/matplotlib 已装入 `.venv`
  - 单元测试通过：`Ran 23 tests ... OK`
  - 全量实验脚本运行成功，已生成 `outputs/tables/*.csv`、`outputs/figures/*.png`、`outputs/reports/实验总览报告.md`
- 遇到的问题与判断：
  - 第一次运行测试时 `np.trapz` 在 numpy 2.4.4 中不可用，已改为 `np.trapezoid`
  - 当前实验输出是“快速默认配置 + surrogate strong phase + 近似 RA-SOMP+MUSIC”下的可运行结果，不能直接写成“已严格复现原论文全部实验曲线”
- 下一步建议：
  - 先查看 `outputs/reports/实验总览报告.md` 和所有输出图
  - 若某张图和原文差距明显，按 `outputs/reports/复现偏差排查入口.md` 从最近日志、config、算法实现、fit_status 逐层排查
  - 如果要拉近论文尺度，把 `configs/*.yaml` 的 `run.n`、`delta_grid`、trial 数和 band 参数逐步调回 `paper_reference`
