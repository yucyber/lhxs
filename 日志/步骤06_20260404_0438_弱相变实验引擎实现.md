# 步骤06：弱相变实验引擎实现

- 时间：2026-04-04 04:38
- 本步骤目标：实现 weak phase transition 的二分搜索、转移带采样、logistic 拟合、原始 trial 落盘和 Fig.3/Fig.7 风格绘图。
- 本步骤新增/修改文件：
  - 新增 `src/joint_sparse_recovery/experiments/weak_phase.py`
  - 新增 `tests/test_weak_phase_pipeline.py`
  - 新增 `日志/步骤06_20260404_0438_弱相变实验引擎实现.md`
- 本步骤执行的命令：
  - 读取原论文 Section III-B 中 weak phase transition、binary search、50-point band sampling、logistic regression 和 stopping criteria 描述
- 自检结果：弱相变实验主流程和测试入口已实现；每条曲线可输出 `rho_w` 和 `fit_status`，失败时不会静默吞掉。
- 遇到的问题与判断：为减少一次性运行耗时，`configs/weak_phase.yaml` 的 `run` 部分默认使用缩小后的 `n` 和 `delta_grid`，但 `paper_reference` 已保留原文正式协议。若后续需要更贴近论文尺度，优先改 config，而不是改算法代码。
- 下一步建议：进入 STEP 7，实现 fixed-vs-normalized 对照实验与 RA-SOMP+MUSIC 对比实验。
