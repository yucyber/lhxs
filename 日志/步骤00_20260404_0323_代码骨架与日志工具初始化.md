# 步骤00：代码骨架与日志工具初始化

- 时间：2026-04-04 03:23
- 本步骤目标：创建 `configs/`、`scripts/`、`src/`、`tests/`、`outputs/` 代码骨架，补最小 Python 包结构，并实现中文步骤日志写入工具。
- 本步骤新增/修改文件：
  - 新增 `src/joint_sparse_recovery/__init__.py`
  - 新增 `src/joint_sparse_recovery/algorithms/__init__.py`
  - 新增 `src/joint_sparse_recovery/core/__init__.py`
  - 新增 `src/joint_sparse_recovery/data/__init__.py`
  - 新增 `src/joint_sparse_recovery/experiments/__init__.py`
  - 新增 `src/joint_sparse_recovery/plotting/__init__.py`
  - 新增 `src/joint_sparse_recovery/theory/__init__.py`
  - 新增 `src/joint_sparse_recovery/logging_utils.py`
  - 新增 `日志/步骤00_20260404_0323_代码骨架与日志工具初始化.md`
- 本步骤执行的命令：
  - `New-Item -ItemType Directory -Force ...`
  - `Get-ChildItem -Force src\joint_sparse_recovery`
- 自检结果：目录骨架已创建，包入口文件已补齐，`logging_utils.py` 已提供 `write_step_log` 和 `append_step_log` 两个最小函数。
- 遇到的问题与判断：本步骤按工作流要求未实现任何算法细节；`configs/`、`scripts/`、`tests/`、`outputs/` 目前只建目录，具体文件留到 STEP 1 以后生成。
- 下一步建议：进入 STEP 1，从 `docs/reference_paper/Greedy_Algorithms_for_Joint_Sparse_Recovery_clean.md` 抽取实验协议并固化到 `configs/*.yaml`。
