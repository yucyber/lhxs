# 步骤08：脚本入口与总控调度实现

- 时间：2026-04-04 04:58
- 本步骤目标：补齐四组实验脚本入口和 `run_all_experiments.py` 总控调度脚本，统一输出图表、报告和运行日志。
- 本步骤新增/修改文件：
  - 新增 `scripts/run_strong_phase.py`
  - 新增 `scripts/run_weak_phase.py`
  - 新增 `scripts/run_step_size_compare.py`
  - 新增 `scripts/run_rank_aware_compare.py`
  - 新增 `scripts/run_all_experiments.py`
  - 修改 `src/joint_sparse_recovery/experiments/__init__.py`
  - 修改 `src/joint_sparse_recovery/plotting/__init__.py`
  - 修改 `src/joint_sparse_recovery/theory/__init__.py`
  - 新增 `日志/步骤08_20260404_0458_脚本入口与总控调度实现.md`
- 本步骤执行的命令：
  - 无额外运行命令；本步骤以代码生成和入口编排为主
- 自检结果：四个单实验脚本和一个总控脚本已写入，均已把 `src/` 注入 `sys.path`，可在未安装包模式下直接从项目根目录运行。
- 遇到的问题与判断：`run_all_experiments.py` 会把 strong/step-size/weak/rank-aware 串行跑完；若某一步失败，会把 traceback 写入 `outputs/reports/实验总览报告.md` 并生成失败日志。
- 下一步建议：进入 STEP 9，安装缺失依赖，运行全部单元测试和全量实验，生成最终自检报告与日志。
